# Compatibility policy

The 0.x line remains additive where practical. Public removals require a replacement and warning
period. Events and replay schemas are independently versioned. Security fixes may reject previously
accepted unsafe configurations. `0.10.0` is a stabilization milestone, not a `1.0.0` declaration.
