# Context and provenance

Every context item identifies source, trust and sensitivity. System items are retained; recent
complete items fill the remaining budget. External documents and tool results remain untrusted
regardless of their wording. Production adapters should use provider token counters and artifacts
for large payloads; the reference selector uses a deterministic character budget.
